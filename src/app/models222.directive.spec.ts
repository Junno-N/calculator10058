import { Models222Directive } from './models222.directive';

describe('Models222Directive', () => {
  it('should create an instance', () => {
    const directive = new Models222Directive();
    expect(directive).toBeTruthy();
  });
});
